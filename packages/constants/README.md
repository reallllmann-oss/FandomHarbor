# Constants Package Boundary

Future home of reviewed, non-secret, cross-application constants such as stable domain codes and shared limits.

Design tokens belong in `packages/ui`, environment configuration belongs in `packages/config`, and user-editable policy must remain data rather than compiled constants.

