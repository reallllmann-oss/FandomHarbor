# Editor Package Boundary

Future home of the shared TipTap document schema, approved extensions, serialization, validation, sanitization, rendering and structured-content migration utilities.

Editor-specific UI may compose `packages/ui`; application routes, publishing workflows and database access do not belong here.

