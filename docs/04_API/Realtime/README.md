# Realtime Registry

No Realtime interface is approved in Phase 0.6.

Future Realtime use requires a documented user need, channel/topic, authorization, event contract, reconnect/deduplication behavior, rate/scale impact, privacy review and fallback. Presence/chat/social-feed behavior is outside current scope.

Realtime must never be mixed into REST documentation or bypass RLS.

