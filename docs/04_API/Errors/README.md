# Error Contract Registry

Stable error codes are defined in `ERROR_CATALOG.md`. Interfaces return safe messages and a request/correlation ID without stack traces, private identity or hidden-resource disclosure.

Field errors follow shared contracts. Transport status mapping is documented by REST; Server Actions use the same domain codes in typed results.

