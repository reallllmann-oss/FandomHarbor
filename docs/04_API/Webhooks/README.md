# Webhook Registry

No webhook is approved in Phase 0.6.

Every future webhook documents provider/direction, signature verification, replay window, idempotency key, payload contract/version, secret rotation, retry/dead-letter behavior, audit and privacy. Webhooks use narrow Route Handlers and never trust provider payloads without validation.
